<UserControl
  x:Class="LingChat.Pages.AboutPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  d:DesignHeight="500"
  d:DesignWidth="800"
  mc:Ignorable="d"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="16" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ui:Card Grid.Row="0" Padding="24">
      <StackPanel>
        <TextBlock
          FontSize="22"
          FontWeight="SemiBold"
          Text="关于项目"
        />
        <TextBlock
          Margin="0,10,0,0"
          Text="LingChat 当前采用 WPF UI 窗口样式，页面导航不依赖 MVVM，而是由注册表和代码后置完成装配。"
          TextWrapping="Wrap"
        />
      </StackPanel>
    </ui:Card>

    <ui:Card Grid.Row="2" Padding="24">
      <StackPanel>
        <TextBlock
          FontSize="20"
          FontWeight="SemiBold"
          Text="结构约定"
        />
        <TextBlock
          Margin="0,10,0,0"
          Text="新增页面时，推荐直接在 Pages 目录创建 XAML 页面及对应的 .xaml.cs 文件，并在 AppPageRegistry 中注册标题、描述和工厂方法。"
          TextWrapping="Wrap"
        />
        <TextBlock
          Margin="0,8,0,0"
          Text="这样主窗口不需要感知页面内部实现，也不用在 XAML 里手工堆内容面板。"
          TextWrapping="Wrap"
        />
      </StackPanel>
    </ui:Card>
  </Grid>
</UserControl>

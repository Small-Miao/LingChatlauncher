<UserControl
  x:Class="LingChat.PageHeaders.AboutPageHeader"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  d:DesignHeight="180"
  d:DesignWidth="800"
  mc:Ignorable="d"
>
  <ui:Card Padding="20">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="20" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <StackPanel>
        <TextBlock
          FontSize="30"
          FontWeight="SemiBold"
          Text="关于"
        />
        <TextBlock
          Margin="0,8,0,0"
          Opacity="0.78"
          Text="这个页面演示了自定义 PageHeader。注册时可以指定单独的头部控件。"
          TextWrapping="Wrap"
        />
      </StackPanel>

      <Border
        Grid.Column="2"
        Padding="14,10"
        Background="{DynamicResource AccentFillColorSecondaryBrush}"
        CornerRadius="12"
      >
        <StackPanel>
          <TextBlock
            FontWeight="SemiBold"
            Text="头部类型"
          />
          <TextBlock
            Margin="0,6,0,0"
            Text="Custom Header"
          />
        </StackPanel>
      </Border>
    </Grid>
  </ui:Card>
</UserControl>
